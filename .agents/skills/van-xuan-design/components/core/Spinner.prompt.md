Loading ring for page-level loaders, button `isLoading` state, and inline async waits.

```jsx
<Spinner size="lg" />
```

Renders as a CSS border-spinner in the brand indigo (no icon dependency) — matches the source's `Loader2` spin visually without requiring the icon library at the primitive level. Sizes: `sm` 16px, `md` 24px, `lg` 32px, `xl` 48px (full-page loaders).
